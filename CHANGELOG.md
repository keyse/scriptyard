# Changelog
All notable Pine script changes documented here. Pair every entry with a screenshot in `notes/`.
## Unreleased
- Initial workspace scaffold.
- 2026-04-30  src/indicators/_smoke_test.pine — Pine v6 smoke-test indicator (EMA + diagnostic table + alertcondition). Round-tripped manually through TradingView Pine Editor on ES1! 5m; compiled clean, EMA + table rendered as expected. Screenshot: notes/smoke-test-2026-04-30.png. (Chrome-MCP automation attempted; aborted at navigate step — tradingview.com is on the extension's deny-list. See notes/smoke-test-FAILURE-2026-04-30T174250Z.txt.)
- 2026-04-30  src/indicators/pullback_indicator.pine v2 — 9/20 EMA pullback entry indicator (HTF EMAs on chart TF). Reverse-engineered spec at specs/pullback_indicator.spec.md. v1 (uploaded source) had a CRITICAL HTF repaint bug — `request.security(..., ta.ema(close, len), lookahead = barmerge.lookahead_off)` without `[1]` offset. v2 fixes: (a) tupled `request.security` with `[1]` + `lookahead_on` per docs/PATTERNS.md non-repaint idiom, (b) full `max_lines_count`/`max_labels_count`/`max_boxes_count` declarations, (c) `i_`-prefixed inputs (PineCoders convention), (d) camelCase internals. Audit: pine-repaint-audit PASS. Round-tripped through TradingView Pine Editor; compiled clean, EMAs + signals rendered as expected. Screenshot: notes/pullback_indicator-2026-04-30.png.
- 2026-04-30  src/strategies/pullback_strat.pine v1 — 9/20 EMA pullback strategy that consumes the indicator's bull/bear signal logic and turns it into orders. Spec: specs/pullback_strat.spec.md. Signal block duplicated verbatim from pullback_indicator.pine v2 ("Inline-during-dev" pattern, docs/ARCHITECTURE.md) — preserves the [1]+lookahead_on non-repaint idiom. Adds: explicit calc-flag matrix (calc_on_every_tick=false, process_orders_on_close=false, fill_orders_on_standard_ohlc=true, $2.50/contract commission, 1 tick slippage, 50/50 margin, pyramiding 0, fixed 1 contract on $25k initial capital), `var float entryStop`/`entryTarget` captured at signal time (frozen for life of trade — no trailing stop), strategy.entry+strategy.exit bracket per side, EOD flatten at 15:55 ET (configurable, IANA tz), pass-through-mode ATR fallback so strategy.exit always gets a concrete target (deviation from indicator's unbounded pass-through, documented in spec), Block-mode soft assert via log.error + entry skip (downgraded from runtime.error after pine-reviewer flagged backtest-halt risk), 10-row dashboard table at position.bottom_right gated by barstate.islast, alertcondition pair at global scope. TODOs in file: RiskLib.sizeByRisk integration once library exists; alert()+JSON webhook refactor when broker bridge goes live. Audit: pine-repaint-audit PASS, pine-reviewer PASS (one WARN resolved), pine-strategy-tester pre-paste 5/5. Round-tripped through TradingView Pine Editor on /MES 1m; compiled clean, dashboard rendered, trades fired with next-bar-open fills as expected. Screenshot: notes/pullback_strat-2026-04-30.png.
